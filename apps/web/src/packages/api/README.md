# API Package Skeleton

- Place generated GraphQL types in `__generated__/`.
- Add API mocks under `mocks/` to decouple UI from live backend.
- Share API-specific helpers (fragments, queries, mutations) for modules.
