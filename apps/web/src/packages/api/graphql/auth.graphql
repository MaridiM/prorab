# ==================== Auth Mutations ====================

mutation Register($input: RegisterInput!) {
  register(input: $input) {
    user {
      id
      email
      fullName
      phone
      emailVerified
      hasCompletedOnboarding
      businessRole
      businessRoleAssignedAt
    }
    message
  }
}

mutation Login($input: LoginInput!) {
  login(input: $input) {
    user {
      id
      email
      fullName
      phone
      emailVerified
      hasCompletedOnboarding
      businessRole
      businessRoleAssignedAt
    }
    requiresTwoFactor
    twoFactorToken
  }
}

mutation VerifyTwoFactorLogin($twoFactorToken: String!, $code: String!) {
  verifyTwoFactorLogin(twoFactorToken: $twoFactorToken, code: $code) {
    user {
      id
      email
      fullName
      phone
      emailVerified
      hasCompletedOnboarding
      businessRole
      businessRoleAssignedAt
    }
  }
}

mutation Logout {
  logout
}

mutation RefreshSession {
  refreshSession {
    user {
      id
      email
      fullName
      phone
      emailVerified
      hasCompletedOnboarding
      businessRole
      businessRoleAssignedAt
    }
  }
}

mutation VerifyEmail($token: String!) {
  verifyEmail(token: $token)
}

mutation ResendVerificationEmail {
  resendVerificationEmail
}

mutation ForgotPassword($email: String!) {
  forgotPassword(email: $email)
}

mutation ResetPassword($input: ResetPasswordInput!) {
  resetPassword(input: $input)
}

mutation ChangePassword($input: ChangePasswordInput!) {
  changePassword(input: $input)
}

mutation UpdateProfile($input: UpdateProfileInput!) {
  updateProfile(input: $input) {
    id
    email
    fullName
    phone
  }
}

mutation UpdateNotificationSettings($input: UpdateNotificationSettingsInput!) {
  updateNotificationSettings(input: $input) {
    appPush
    appEmail
    appSms
    marketingPush
    marketingEmail
  }
}

# ==================== Session Mutations ====================

mutation RevokeSession($sessionId: String!) {
  revokeSession(sessionId: $sessionId)
}

mutation RevokeAllSessions {
  revokeAllSessions
}

mutation RevokeAllSessionsIncludingCurrent {
  revokeAllSessionsIncludingCurrent
}

# ==================== Telegram OAuth ====================

mutation InitTelegramAuth {
  initTelegramAuth {
    token
    deepLink
    expiresAt
  }
}

mutation CheckTelegramAuth($input: CheckTelegramAuthInput!) {
  checkTelegramAuth(input: $input) {
    completed
    user {
      id
      email
      fullName
      phone
      emailVerified
      hasCompletedOnboarding
      businessRole
      businessRoleAssignedAt
    }
    sessionToken
    refreshToken
  }
}

# ==================== Queries ====================

query Me {
  me {
    id
    email
    fullName
    phone
    avatarUrl
    emailVerified
    hasCompletedOnboarding
    businessRole
    businessRoleAssignedAt
    createdAt
    notificationSettings {
      appPush
      appEmail
      appSms
      marketingPush
      marketingEmail
    }
    adminRole {
      id
      role
      permissions
    }
  }
}

query Sessions {
  sessions {
    id
    userAgent
    ip
    createdAt
    current
  }
}

