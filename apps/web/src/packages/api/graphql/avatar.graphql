mutation UploadAvatar($file: Upload!) {
  uploadAvatar(file: $file) {
    id
    avatarUrl
    fullName
    email
  }
}

mutation DeleteAvatar {
  deleteAvatar {
    id
    avatarUrl
    fullName
    email
  }
}
