# ==================== TYPES ====================
# Note: Most types are defined in schema.gql generated from backend
# This file only includes client-specific queries and mutations

# ==================== QUERIES ====================

query MyTeams {
  myTeams {
    id
    name
    logoType
    logoUrl
    iconId
    colorId
    ownerId
    createdAt
    updatedAt
  }
}

# ==================== MUTATIONS ====================

mutation CompleteOnboarding(
  $input: CompleteOnboardingInput!
) {
  completeOnboarding(input: $input) {
    success
    message
    team {
      id
      name
      logoType
      logoUrl
      iconId
      colorId
      createdAt
    }
    project {
      id
      name
      address
      description
      isActive
      createdAt
    }
  }
}

# TODO: Implement these mutations on backend
# mutation UpdateTeamLogo
# mutation CreateInviteCode
# mutation JoinTeamByInvite
# mutation CreateTeam
# mutation UpdateTeam
# mutation CreateProject
# mutation UpdateProject
