# Components Package Skeleton

- `shared/ui`: base UI atoms (e.g., Button, Input, Card).
- `features`: composite UI for specific flows.
- `widgets`: layout-level composites (headers, navs, footers).

Use this package to host reusable UI that can be shared across modules.
