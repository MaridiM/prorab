# Libs Package Skeleton

- `apollo`: client setup and helpers (already configured under `src/lib/apollo`).
- `i18n`: locale utilities and types (to add when localization appears).
- `store`: Zustand or other state slices per domain.
- `hooks`: cross-cutting React hooks not tied to a single module.
